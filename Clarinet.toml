[project]
name = "configure-state"
authors = []
description = "Decentralized network configuration management on Stacks"
telemetry = false
requirements = []
[contracts.configuration-manager]
path = "contracts/configuration-manager.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
